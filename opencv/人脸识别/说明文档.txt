人脸识别原理：
	算法是基于计算机开源的视觉库的，本程序也是开源程序（自己添加了邮件发送程序），大致原理就是先运行FaceDataCollect.py，进行人脸数据采集，再运行face_training.py对刚才采集到的图片进行数据训练得到区分人脸的数据，最后运行face_recognition.py检测镜头前的人脸去匹配训练中的数据，将匹配结果输出到屏幕上或者邮件发送未知的面孔。


注意：运行程序前一定要安装导入opencv(人脸识别中有，也可自己用命令下载安装），若程序运行报错，请检查程序中提示的路径，那是算法的关键，根据个人的路径来填写。